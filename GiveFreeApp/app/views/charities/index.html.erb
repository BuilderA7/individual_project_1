      <form class="navbar-form navbar-left">

      <%=link_to "advanced search",search_index_path  %>
      </form><br><br>

<h1>Our Partners</h1>

<table>
  <thead>
    <tr>
      <th>Orginizations</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
   <% @charities.each do |charity| %>
      <tr>
        <td><%= charity.name%></td>
        <td>  <%= link_to 'information', charity %>  </td>
<td><%if current_user == true && current_user.email == "admin@gmail.com" %>
           <%= link_to 'Destroy', charity_path(charity.id), method: :delete, data: { confirm: 'Are you sure?' } %>
           <% end %>
        </td>
      </tr>
     <% end %> 
  </tbody>
</table>
      

<br>



